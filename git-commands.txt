git init-- you can initialized empty repo
git status -- you can check file status
git add -- adding files stage is changing from untracked to stage  you can not tracked file on stage mode clear  
git commit -m "Anything Write" -- stage is changing from stage to Tracked
git restore-- If you delete file you can store when the file is in tracked already

git remote add origin https://github.com/somingi5/github-for-devops.git
git remote -v -- 
git remote set-url origin https://ghp_xxxxxxxxxxxxxxxxxxxxxxxxxx@github.com/soming5/github-for-devops.git
git push origin master

## Check currently username password on Git below commands run on Local
Example-- Prince

git config --global user.name
git config --global user.email

## Set Username & Email on Local    

git config --global user.name "Your Github Username"
git config --global user.email "YourGithub@gmail.com"

# Verify

git config --global --list

this is my commands updated hi hello how are you

## Branches

Main master dev are branches name
git branch -- show branch
git branch dev  --- dev branch created 
git switch dev --- Switching branch git checkout and git switch are same 
git log -- check latest commit
git log --oneline

# Head represent the latest commit on your repository

# suppose if we want to push dev new branch on Github so how can i push it

go inside the branch and check status
git push origin dev -- created new dev branch on Github and push 

#branch merging master and dev
go to pull request and click new pull request

# Pull request It a way of merging one branch to another branch on github

Base is master and compare is dev after create pull request
click on create pull request
click on merge pull request
show on repository dev changes are visible on master 

git switch master 
git pull origin master 
changes show on Local vs code

# fetch vs Pull
git fetch -- if you want all branches from your github in your local vs code use git fetch
git pull -- if you want remote changes and any branch specific changes from your Github(Origin)
to in your local VS Code then you will use git pull origin

Head- it shows the latest commit